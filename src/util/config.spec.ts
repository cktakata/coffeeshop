import { Config } from "./config";

describe('Config', () => {
    let config: Config;

    beforeEach(async() => {
        config = new Config();
    })

    describe('root', () => {
        it('should test config', async() => {
        })
    })
})